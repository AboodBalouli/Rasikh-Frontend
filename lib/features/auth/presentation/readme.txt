Inside presentation:

UI Pages → screens

Widgets → buttons, forms, grids

State management → controllers, providers, blocs

Rule:
No HTTP, no JSON, no business logic in UI.

UI only calls usecases or controllers.